# AxePilot — Agent Instructions

This is the [agents.md](https://agents.md) cross-tool standard entry point for AI coding agents. Canonical duplicate that other tool-specific pointer files (`CLAUDE.md`, `.agents/AGENTS.md`, `.github/copilot-instructions.md`, `.devin/rules/`) point back to.

- **Process, workflow, and safety rules**: [`RULES.md`](RULES.md) — the single canonical source. Read it before making any change, in any language, to this repo.
- **Architecture, components, and continuation guidance**: [`CONTINUATION_PROMPT.md`](CONTINUATION_PROMPT.md).
- **Security threat model and accepted risks**: [`SECURITY.md`](SECURITY.md).

This file is a pointer only — do not duplicate rule text here. If you're about to copy a rule from `RULES.md` into this file "for convenience," don't; update `RULES.md` instead and let every pointer stay thin.
